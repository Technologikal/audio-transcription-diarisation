# Environment variables (contains secrets!)
.env
*.env
!.env.example

# Docker build cache (models copied from ~/.cache/huggingface/hub/)
hf_cache/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv

# Temporary files
temp_chunk_*.wav
*.tmp

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Large audio files (optional - comment out if you want to track them)
*.m4a
*.wav
*.mp3
*.flac
*.tar.gz
*.zip

# Documents (may contain private meeting content)
*.doc
*.docx
*.pdf

# Output files (transcription results)
# NOTE: *_transcription.txt does NOT match the *_transcript.txt files the
# pipeline actually writes — both patterns are needed (027/US5 T006).
*_transcription.txt
*_transcript.txt
*_summary.md
transcription_output.txt
output.txt
